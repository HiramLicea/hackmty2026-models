# Runtime
APP_ENV=development
LOG_LEVEL=INFO
DOCS_ENABLED=true

# Shared credential for trusted callers of the inference-only API.
INFERENCE_API_KEY=

# Read-only model files packaged with the deployment.
MODEL_ARTIFACT_DIR=artifacts
MODEL_MANIFEST_PATH=artifacts/manifest.json
